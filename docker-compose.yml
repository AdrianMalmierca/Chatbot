services:
  backend:
    build: ./es/upsa/tfg
    ports:
      - "8000:8000"
    environment:
      - OPENAI_API_KEY=${OPENAI_API_KEY}

  frontend:
    build: ./frontend
    ports:
      - "4200:80"
    depends_on:
      - backend